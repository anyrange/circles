{
  "name": "circles",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "scripts": {
    "database": "pnpm --filter database",
    "web": "pnpm --filter web",
    "worker": "pnpm --filter worker",
    "api": "pnpm --filter spotifyAPI",
    "preinstall": "npx only-allow pnpm",
    "postinstall": "pnpm --filter database generate"
  },
  "packageManager": "pnpm@7.25.1",
  "engines": {
    "node": ">=16.14.0"
  },
  "devDependencies": {
    "rome": "^11.0.0"
  }
}
