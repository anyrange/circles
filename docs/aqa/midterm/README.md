# AQA Midterm

This directory contains the standalone midterm technical report for the AQA course.

Files:

- `report.typ` — Typst source
- `report.pdf` — compiled report
- `package.json` — local Typst build scripts

Build:

```sh
typst compile report.typ report.pdf
```
