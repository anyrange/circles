# Implementation Plans

Generated by the improve skill on 2026-06-10 from commit `5727da7`, with shadcn follow-up plans added on 2026-06-11 from commit `f668e4f`. Execute in the order below unless dependencies say otherwise. Each executor should read the plan fully before starting, honor STOP conditions, and update the status row when done.

## Execution order & status

| Plan | Title                                            | Priority | Effort | Depends on | Status |
| ---- | ------------------------------------------------ | -------- | ------ | ---------- | ------ |
| 001  | Remove session tokens from URLs and localStorage | P1       | M      | —          | DONE   |
| 002  | Bind import processing to owned upload keys      | P1       | M      | —          | DONE   |
| 004  | Add API and workflow verification to CI          | P1       | M      | —          | DONE   |
| 003  | Stream and limit Spotify import processing       | P2       | L      | 002        | DONE   |
| 005  | Use shadcn Button primitives on the landing page | P2       | S      | —          | TODO   |
| 006  | Use semantic shadcn feedback in import uploader  | P2       | S      | —          | TODO   |
| 007  | Consolidate empty states with shadcn Empty       | P3       | S      | —          | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Vetted Findings

| #   | Finding                                                                              | Category | Impact                                                                                               | Effort | Risk | Evidence                                                                                                                                                                   |
| --- | ------------------------------------------------------------------------------------ | -------- | ---------------------------------------------------------------------------------------------------- | ------ | ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Session token is moved from an HttpOnly cookie into URL/localStorage bearer auth     | security | Session credentials can leak via URL/history/logs and become script-readable                         | M      | MED  | `apps/backend/src/api/index.ts:36`, `apps/frontend/src/lib/session-token.ts:8`, `apps/frontend/src/lib/api.ts:11`                                                          |
| 2   | Import processing trusts a client-supplied S3 key                                    | security | Authenticated users can ask workers to fetch arbitrary bucket keys rather than only their own upload | M      | MED  | `apps/backend/src/api/controllers/import.ts:42`, `apps/backend/src/api/controllers/import.ts:46`, `apps/backend/src/worker/workflows/process-import.ts:47`                 |
| 3   | CI does not run tests or builds, and backend coverage excludes controllers/workflows | tests    | Auth, import, and worker regressions can pass required PR checks                                     | M      | LOW  | `.github/workflows/ci.yml:20`, `package.json:6`, `apps/backend/vite.config.ts:18`                                                                                          |
| 4   | Spotify import reads and rewrites whole datasets repeatedly                          | perf     | Large exports can cause memory spikes and repeated S3/JSON parse work per batch                      | L      | MED  | `apps/backend/src/worker/workflows/process-import.ts:48`, `apps/backend/src/worker/workflows/process-import.ts:53`, `apps/backend/src/worker/workflows/import-batch.ts:47` |

## Architecture Candidates

1. **Import module depth**: `importController`, `processImport`, and `importBatch` expose storage keys and batch mechanics across several modules. Deepen the import module so the interface is "owned upload -> bounded batches -> history rows"; this improves locality for ownership, size limits, status transitions, and tests.
2. **Auth module seam**: browser session handling is split between Better Auth cookies, direct session-table bearer lookup, URL token handoff, and localStorage. Collapse the interface to cookie-backed browser sessions, and introduce a separate adapter only if non-browser clients need tokens later.
3. **Verification module/interface**: CI currently treats formatting/linting as the verification interface. Make `verify` a deep repo-level module hiding the right static, test, and build gates behind one command.

## Dependency notes

- Plan 003 depends on Plan 002 because import batching should be redesigned after the upload/job ownership contract is fixed.
- Plan 004 can run before or after Plans 001/002, but its best test targets are the auth and import seams changed by those plans.
- Plans 005, 006, and 007 are independent shadcn cleanup plans. Plan 005 is the highest-leverage UI cleanup because it removes duplicated CTA button styling from the first screen.

## Shadcn Follow-up Findings

| #   | Finding                                                     | Category  | Impact                                                                                         | Effort | Risk | Evidence                                                                                                                                                                                     |
| --- | ----------------------------------------------------------- | --------- | ---------------------------------------------------------------------------------------------- | ------ | ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 5   | Landing page bypasses installed Button and Avatar patterns  | tech-debt | Button styling, icon sizing, raw colors, and spacing rules drift from the shadcn component kit | S      | LOW  | `apps/frontend/src/routes/index.tsx:49`, `apps/frontend/src/routes/index.tsx:76`, `apps/frontend/src/routes/index.tsx:84`                                                                    |
| 6   | Import uploader uses custom feedback boxes instead of Alert | tech-debt | Upload errors and completion states have inconsistent semantics and styling                    | S      | LOW  | `apps/frontend/src/components/import-uploader.tsx:72`, `apps/frontend/src/components/import-uploader.tsx:94`, `apps/frontend/src/components/import-uploader.tsx:101`                         |
| 7   | Empty states are custom muted paragraphs                    | tech-debt | No-data UI differs across profile and feed surfaces despite shadcn Empty fitting the use case  | S      | LOW  | `apps/frontend/src/routes/_authenticated/u/$username/route.tsx:239`, `apps/frontend/src/routes/_authenticated/u/$username/route.tsx:371`, `apps/frontend/src/components/feed-section.tsx:27` |

## Findings considered and rejected

- `dangerouslySetInnerHTML` in `apps/frontend/src/routes/__root.tsx:47`: rejected as a security finding because the injected script is a static theme bootstrap string, not user-controlled input.
- shadcn spacing/style nits such as `-space-x-2` in the landing page and raw CTA button styling: originally deferred behind auth/import/CI risks; now planned in `plans/005-use-shadcn-buttons-on-landing.md`.
- Tracked `apps/frontend/.env.production`: deferred. The visible key is a public `VITE_API_URL`; no secret value was copied into this report.
- Better Auth `skipStateCookieCheck` in `apps/backend/src/library/auth.ts:23`: investigate later. It is security-relevant, but the current URL/localStorage token handoff is the higher-confidence immediate issue.

## Not audited

- I did not run mutating setup (`vp install`) or formatter commands because this was a read-only improve audit.
- I did not execute the full test/build suite; the current CI configuration itself is a finding.
- I did not audit deployment infrastructure outside files present in this repository.
