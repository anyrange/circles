{
  "name": "@circles/database",
  "main": "src/index.ts",
  "version": "0.0.1",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "generate": "drizzle-kit generate:pg --schema=./src/schema.ts --out=./drizzle",
    "migrate": "dotenv -e ../../.env -- ts-node ./src/scripts/migrate.ts",
    "print-info": "dotenv -e ../../.env -- ts-node ./src/scripts/info.ts",
    "flush": "dotenv -e ../../.env -- ts-node ./src/scripts/flush.ts"
  },
  "dependencies": {
    "@circles/types": "*",
    "@prisma/client": "^4.10.1",
    "drizzle-orm": "^0.27.0",
    "postgres": "^3.3.5"
  },
  "devDependencies": {
    "@circles/tsconfig": "*",
    "@types/node": "^20.4.1",
    "dotenv-cli": "^7.0.0",
    "drizzle-kit": "^0.19.3",
    "ts-node": "^10.9.1"
  }
}
